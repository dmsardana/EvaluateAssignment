anthropic>=0.40.0
# Multi-provider LLM routing — Phase 2 of the credential-registry work.
# Both are lazy-imported inside tools/evaluate_pdf.py so an Anthropic-only
# install keeps working until the operator pip-installs these.
google-genai>=0.5.0
openai>=1.50.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0
python-dotenv>=1.0.0
jinja2>=3.1.0
# Required by FastAPI's File/UploadFile for the manual answer-key upload route.
python-multipart>=0.0.20
# Postgres driver — bundles libpq, no host install needed.
psycopg[binary,pool]>=3.2.0
# Background credential health checks (15-min interval).
apscheduler>=3.10.4
